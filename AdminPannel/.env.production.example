# API base URL is resolved in src/api.js (no VITE_* vars needed).
#
# Production build is deployed at the domain web root (admin panel).
# Run: npm run build
# Upload dist/* to the server's web root.
