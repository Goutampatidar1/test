# Optional API override (restart `npm run dev` after changing).
# Use your PC's LAN IP when testing registration on a phone, e.g.:
# VITE_API_URL=http://192.168.1.5:5001
#
# If unset, api.js uses http://localhost:5001 on desktop and
# http(s)://<same-host-as-page>:5001 on mobile/LAN.
