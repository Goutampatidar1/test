# API base URL baked in at build time (required for production).
VITE_API_URL=https://api.yourdomain.com

# Production build is deployed under /vendor/ on the same host as the admin panel.
# Run: npm run build
# Upload dist/* to the server's /vendor/ directory.
